server:
  port: 8080
#server:
#  port: 8443
#  ssl:
#    enabled: true
#    key-store: classpath:keystore/customer-service-cert.p12
#    key-store-password: ${SSL_KEYSTORE_PASSWORD}
#    key-store-type: PKCS12
#    key-alias: customer-service-cert

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        default_schema: customer

  liquibase:
    enabled: true
    change-log: db/changelog-cumulative.yaml
    default-schema: customer
    contexts: development